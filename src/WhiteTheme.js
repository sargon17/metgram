import { createTheme } from "@mui/material";

const whiteTheme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#F9F9F9;",
    },
  },
});

export default whiteTheme;
